# Subtask 005-04-01: articles repository/service

## 概要

articles ドメインのDB操作層とビジネスロジック層を実装する。
**セマンティック検索（意味的類似度検索）** を提供し、ファジーなクエリにも対応する。

### セマンティック検索とは

SQLの`LIKE`や全文検索ではなく、Embeddingによる意味的類似度検索を行う。

```
ユーザー入力: "ホラー系のscp"
       ↓
① OpenAI Embeddings API でベクトル化
       ↓
② pgvector コサイン類似度検索
       ↓
意味的に類似した記事を返す
```

これにより「ホラー」という単語が含まれていなくても、
「怖い」「不気味」などの意味的に類似した記事がヒットする。

## ステータス

- **status**: completed

## ユーザーストーリー

**As a** API開発者
**I want** セマンティック検索ロジックを持つ
**So that** ユーザーのファジーなクエリにも対応できる

## Acceptance Criteria（EARS記法）

- [x] WHEN ArticlesRepositoryをインスタンス化した際
      THEN searchByEmbedding, getArticleByIdメソッドを提供する

- [x] WHEN ArticlesServiceをインスタンス化した際
      THEN searchArticlesメソッドを提供する

- [x] WHEN searchArticles を呼び出した際
      GIVEN 有効なクエリ文字列の場合
      THEN クエリをOpenAI Embedding API（text-embedding-3-small）でベクトル化する
      AND pgvector RPC関数で類似記事を検索する
      AND 類似度降順でソートされた結果を返す

- [x] WHEN searchArticles を呼び出した際
      GIVEN 日本語・英語どちらのクエリでも
      THEN 意味的に類似した記事を返す（多言語対応）

## 技術設計

### repository.ts

```typescript
import type { SupabaseClient } from "@supabase/supabase-js";
import type { VectorSearchResult } from "@recommend-scp/shared";

export interface ArticleDetail {
  id: string;
  title: string;
  url: string;
  tags: string[];
  rating: number | null;
}

export class ArticlesRepository {
  constructor(private readonly supabase: SupabaseClient) {}

  async searchByEmbedding(
    queryVector: number[],
    options: { limit?: number; excludeIds?: string[] } = {}
  ): Promise<VectorSearchResult[]> {
    const { data, error } = await this.supabase.rpc("search_articles_by_embedding", {
      query_vector: queryVector,
      exclude_ids: options.excludeIds ?? [],
      match_count: options.limit ?? 10,
    });

    if (error) throw error;
    return data ?? [];
  }

  async getArticleById(id: string): Promise<ArticleDetail | null> {
    const { data, error } = await this.supabase
      .from("scp_articles")
      .select("id, title, url, tags, rating")
      .eq("id", id)
      .single();

    if (error?.code === "PGRST116") return null;
    if (error) throw error;
    return data;
  }
}
```

### service.ts

```typescript
import { ArticlesRepository } from "./repository";
import { createEmbedding } from "../../lib/openai";

export interface SearchArticlesResult {
  articles: Array<{
    id: string;
    title: string;
    similarity: number;
    tags?: string[];
  }>;
  total: number;
  query: string;
}

export class ArticlesService {
  constructor(private readonly repository: ArticlesRepository) {}

  async searchArticles(
    query: string,
    options: { limit?: number } = {}
  ): Promise<SearchArticlesResult> {
    // クエリをEmbeddingに変換
    const queryVector = await createEmbedding(query);

    // ベクトル検索
    const results = await this.repository.searchByEmbedding(queryVector, {
      limit: options.limit ?? 10,
    });

    return {
      articles: results.map((r) => ({
        id: r.id,
        title: r.title,
        similarity: r.similarity,
      })),
      total: results.length,
      query,
    };
  }
}
```

### lib/openai.ts

```typescript
import OpenAI from "openai";
import { env } from "@recommend-scp/shared";

const openai = new OpenAI({ apiKey: env.OPENAI_API_KEY });

export async function createEmbedding(text: string): Promise<number[]> {
  const response = await openai.embeddings.create({
    model: "text-embedding-3-small",
    input: text,
  });
  return response.data[0].embedding;
}
```

## テストケース

- [x] searchByEmbedding でベクトル検索結果を取得できる
- [x] searchArticles でテキストクエリから検索できる
- [x] getArticleById で記事詳細を取得できる
- [x] 存在しない記事IDでnullを返す

## 成果物

- `apps/api-server/src/domains/articles/repository.ts`
- `apps/api-server/src/domains/articles/service.ts`
- `apps/api-server/src/lib/openai.ts`

## 依存関係

- 005-02-03: SupabaseVectorSearch
- 005-02-04: ベクトル検索RPC関数
