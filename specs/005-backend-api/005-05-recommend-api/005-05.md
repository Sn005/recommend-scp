# Story 005-05: recommend API

## 概要

推薦取得エンドポイント（POST /recommend）を実装し、RecommendationEngineと連携する。

## ステータス

- **status**: planned

## ユーザーストーリー

**As a** ユーザー
**I want** POST /recommend で推薦記事を取得できる
**So that** 自分の好みに合った記事を発見できる

## Acceptance Criteria（Story レベル）

- [ ] AC-1: POST /recommend で推薦記事リストを取得できる
- [ ] AC-2: 80/20ルールで好み推薦とセレンディピティ推薦を返す
- [ ] AC-3: オンボーディング未完了時は専用エラーを返す
- [ ] AC-4: 200ms以内にレスポンスを返す

## Subtask 一覧

| ID                          | 名前              | 概要                     | ステータス |
| --------------------------- | ----------------- | ------------------------ | ---------- |
| [005-05-01](./005-05-01.md) | recommend service | RecommendationEngine連携 | planned    |
| [005-05-02](./005-05-02.md) | POST /recommend   | 推薦取得エンドポイント   | planned    |

## 技術設計

### RecommendationEngine連携

```typescript
// packages/sharedのRecommendationEngineを使用
import { RecommendationEngine } from "@recommend-scp/shared";
import { SupabasePreferenceStorage } from "../../lib/storage/supabase-preference-storage";
import { SupabaseVectorSearch } from "../../lib/search/supabase-vector-search";

export class RecommendService {
  private engine: RecommendationEngine;

  constructor(supabase: SupabaseClient) {
    const storage = new SupabasePreferenceStorage(supabase);
    const vectorSearch = new SupabaseVectorSearch(supabase);
    this.engine = new RecommendationEngine(storage, vectorSearch);
  }

  async getRecommendations(visitorId: string, limit: number) {
    return this.engine.getRecommendations(visitorId, limit);
  }
}
```

## 依存関係

- 005-02: サーバー側ストレージ（SupabasePreferenceStorage, SupabaseVectorSearch）
- 005-03: visitors API（visitorId検証）
- EPIC-004: RecommendationEngine
