# Subtask 005-02-01: DBスキーマ定義

## 概要

visitors関連テーブル（visitors, view_history, feedback, recommendation_log, favorites）の
Supabaseマイグレーションを作成する。

## ステータス

- **status**: completed

## ユーザーストーリー

**As a** バックエンド開発者
**I want** ユーザー嗜好データを保存するテーブルを持つ
**So that** 推薦ロジックのデータを永続化できる

## Acceptance Criteria（EARS記法）

### テーブル定義

- [x] WHEN マイグレーションを実行した際
      THEN visitorsテーブルが以下のカラムで作成される: - id: UUID PRIMARY KEY - visitor_id: TEXT UNIQUE NOT NULL - user_id: UUID（将来のAuth連携用、nullable）- preference_vector: vector(1536) - tag_weights: JSONB - object_class_preference: JSONB - starter_pack: TEXT - onboarding_completed_at: TIMESTAMPTZ - created_at, updated_at: TIMESTAMPTZ

- [x] WHEN マイグレーションを実行した際
      THEN view_historyテーブルが以下のカラムで作成される: - id: UUID PRIMARY KEY - visitor_id: TEXT REFERENCES visitors(visitor_id) - article_id: TEXT NOT NULL - viewed_at: TIMESTAMPTZ - duration: INTEGER（秒）

- [x] WHEN マイグレーションを実行した際
      THEN feedbackテーブルが以下のカラムで作成される: - id: UUID PRIMARY KEY - visitor_id: TEXT REFERENCES visitors(visitor_id) - article_id: TEXT NOT NULL - type: TEXT CHECK (type IN ('like', 'dislike')) - created_at: TIMESTAMPTZ - UNIQUE (visitor_id, article_id)

- [x] WHEN マイグレーションを実行した際
      THEN recommendation_logテーブルが以下のカラムで作成される: - id: UUID PRIMARY KEY - visitor_id: TEXT REFERENCES visitors(visitor_id) - article_id: TEXT NOT NULL - source: TEXT CHECK (source IN ('preference', 'serendipity')) - recommended_at: TIMESTAMPTZ - clicked: BOOLEAN

- [x] WHEN マイグレーションを実行した際
      THEN favoritesテーブルが以下のカラムで作成される: - id: UUID PRIMARY KEY - visitor_id: TEXT REFERENCES visitors(visitor_id) - article_id: TEXT NOT NULL - added_at: TIMESTAMPTZ - UNIQUE (visitor_id, article_id)

### 整合性

- [x] WHEN マイグレーションを実行した際
      THEN 全テーブルが正常に作成される
      AND 外部キー制約が正しく設定される

## 技術設計

### マイグレーションファイル

```sql
-- supabase/migrations/YYYYMMDDHHMMSS_create_visitor_tables.sql

-- visitors テーブル
CREATE TABLE visitors (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  visitor_id TEXT UNIQUE NOT NULL,
  user_id UUID REFERENCES auth.users ON DELETE SET NULL,
  preference_vector vector(1536),
  tag_weights JSONB DEFAULT '{}',
  object_class_preference JSONB DEFAULT '{}',
  starter_pack TEXT CHECK (starter_pack IN ('horror', 'surreal', 'scientific', 'heartwarming', 'mystery', 'custom')),
  onboarding_completed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now() NOT NULL,
  updated_at TIMESTAMPTZ DEFAULT now() NOT NULL
);

-- view_history テーブル
CREATE TABLE view_history (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  visitor_id TEXT NOT NULL REFERENCES visitors(visitor_id) ON DELETE CASCADE,
  article_id TEXT NOT NULL,
  viewed_at TIMESTAMPTZ DEFAULT now() NOT NULL,
  duration INTEGER
);

-- feedback テーブル
CREATE TABLE feedback (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  visitor_id TEXT NOT NULL REFERENCES visitors(visitor_id) ON DELETE CASCADE,
  article_id TEXT NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('like', 'dislike')),
  created_at TIMESTAMPTZ DEFAULT now() NOT NULL,
  UNIQUE (visitor_id, article_id)
);

-- recommendation_log テーブル
CREATE TABLE recommendation_log (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  visitor_id TEXT NOT NULL REFERENCES visitors(visitor_id) ON DELETE CASCADE,
  article_id TEXT NOT NULL,
  source TEXT NOT NULL CHECK (source IN ('preference', 'serendipity')),
  recommended_at TIMESTAMPTZ DEFAULT now() NOT NULL,
  clicked BOOLEAN DEFAULT false NOT NULL
);

-- favorites テーブル
CREATE TABLE favorites (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  visitor_id TEXT NOT NULL REFERENCES visitors(visitor_id) ON DELETE CASCADE,
  article_id TEXT NOT NULL,
  added_at TIMESTAMPTZ DEFAULT now() NOT NULL,
  UNIQUE (visitor_id, article_id)
);

-- インデックス
CREATE INDEX idx_view_history_visitor ON view_history(visitor_id);
CREATE INDEX idx_view_history_article ON view_history(article_id);
CREATE INDEX idx_feedback_visitor ON feedback(visitor_id);
CREATE INDEX idx_feedback_type ON feedback(visitor_id, type);
CREATE INDEX idx_recommendation_log_visitor ON recommendation_log(visitor_id);
CREATE INDEX idx_favorites_visitor ON favorites(visitor_id);

-- updated_at 自動更新トリガー
CREATE OR REPLACE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER visitors_updated_at
  BEFORE UPDATE ON visitors
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at();
```

### RLS (Row Level Security) 設計

```sql
-- MVP時点ではRLS無効（visitor_idによる自己制限で十分）
-- 将来のuser_id連携時にRLSを有効化予定

-- 例: 将来のRLS設定
-- ALTER TABLE visitors ENABLE ROW LEVEL SECURITY;
-- CREATE POLICY "Users can view own data" ON visitors
--   FOR SELECT USING (auth.uid() = user_id);
```

## テストケース

- [x] visitorsテーブルが作成される
- [x] view_historyテーブルが作成される
- [x] feedbackテーブルが作成される
- [x] recommendation_logテーブルが作成される
- [x] favoritesテーブルが作成される
- [x] 外部キー制約が正しく動作する
- [x] visitor削除時にカスケード削除される
- [x] feedbackのUNIQUE制約が動作する

## 成果物

- `supabase/migrations/20250120000001_create_visitor_tables.sql`
- `supabase/__tests__/visitor-tables.test.ts`

## 依存関係

- 005-01-01: Honoサーバー初期化（Supabaseクライアント設定）
