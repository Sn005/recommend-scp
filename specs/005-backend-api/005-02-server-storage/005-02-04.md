# Subtask 005-02-04: ベクトル検索RPC関数

## 概要

pgvectorを使用したベクトル類似度検索のSQL RPC関数を作成する。

## ステータス

- **status**: planned

## ユーザーストーリー

**As a** API開発者
**I want** 高速なベクトル検索RPC関数を持つ
**So that** 推薦APIで100ms以内のレスポンスを実現できる

## Acceptance Criteria（EARS記法）

- [ ] WHERE search_articles_by_embedding関数
      THE SYSTEM SHALL 以下のパラメータを受け取る: - query_vector: vector(1536) - exclude_ids: text[] - match_count: integer - min_similarity: float（デフォルト: 0）- max_similarity: float（デフォルト: 1）

- [ ] WHEN search_articles_by_embedding を呼び出した際
      THEN コサイン類似度で検索する
      AND exclude_idsの記事を除外する
      AND 類似度降順でmatch_count件を返す
      AND HNSWインデックスを使用する

- [ ] WHERE search_articles_by_unexplored_tags関数
      THE SYSTEM SHALL 以下のパラメータを受け取る: - explored_tags: text[] - exclude_ids: text[] - match_count: integer - order_by: text（'rating' or 'random'）

- [ ] WHEN search_articles_by_unexplored_tags を呼び出した際
      THEN explored_tagsに含まれないタグを持つ記事を検索する
      AND order_byに応じてソートする

- [ ] WHILE RPC関数を実行する際
      THE SYSTEM SHALL 100ms以内に結果を返す

## 技術設計

### マイグレーションファイル

```sql
-- supabase/migrations/YYYYMMDDHHMMSS_create_search_rpc_functions.sql

-- ベクトル類似度検索
CREATE OR REPLACE FUNCTION search_articles_by_embedding(
  query_vector vector(1536),
  exclude_ids text[] DEFAULT '{}',
  match_count integer DEFAULT 10,
  min_similarity float DEFAULT 0,
  max_similarity float DEFAULT 1
)
RETURNS TABLE (
  id text,
  title text,
  similarity float
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT
    a.id::text,
    a.title,
    1 - (a.embedding <=> query_vector) AS similarity
  FROM scp_articles a
  WHERE a.embedding IS NOT NULL
    AND NOT (a.id = ANY(exclude_ids))
    AND (1 - (a.embedding <=> query_vector)) >= min_similarity
    AND (1 - (a.embedding <=> query_vector)) <= max_similarity
  ORDER BY a.embedding <=> query_vector
  LIMIT match_count;
END;
$$;

-- 未探索タグ検索
CREATE OR REPLACE FUNCTION search_articles_by_unexplored_tags(
  explored_tags text[] DEFAULT '{}',
  exclude_ids text[] DEFAULT '{}',
  match_count integer DEFAULT 10,
  order_by text DEFAULT 'rating'
)
RETURNS TABLE (
  id text,
  title text,
  similarity float
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT
    a.id::text,
    a.title,
    0.5::float AS similarity  -- 固定値
  FROM scp_articles a
  WHERE a.tags IS NOT NULL
    AND NOT (a.id = ANY(exclude_ids))
    AND NOT (a.tags && explored_tags)  -- タグが重複しない
  ORDER BY
    CASE WHEN order_by = 'rating' THEN a.rating END DESC NULLS LAST,
    CASE WHEN order_by = 'random' THEN random() END
  LIMIT match_count;
END;
$$;

-- 隣接領域検索（セレンディピティ用）
CREATE OR REPLACE FUNCTION search_adjacent_articles(
  query_vector vector(1536),
  exclude_ids text[] DEFAULT '{}',
  match_count integer DEFAULT 10,
  min_similarity float DEFAULT 0.3,
  max_similarity float DEFAULT 0.7
)
RETURNS TABLE (
  id text,
  title text,
  similarity float
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT * FROM search_articles_by_embedding(
    query_vector,
    exclude_ids,
    match_count,
    min_similarity,
    max_similarity
  );
END;
$$;
```

### HNSWインデックス確認

EPIC-003で作成済みのインデックスを確認:

```sql
-- 既存のインデックス確認
SELECT indexname, indexdef
FROM pg_indexes
WHERE tablename = 'scp_articles'
AND indexname LIKE '%embedding%';

-- 必要に応じてHNSWインデックスを作成
-- CREATE INDEX IF NOT EXISTS scp_articles_embedding_hnsw_idx
--   ON scp_articles
--   USING hnsw (embedding vector_cosine_ops)
--   WITH (m = 16, ef_construction = 64);
```

## テストケース

- [ ] search_articles_by_embedding が類似記事を返す
- [ ] search_articles_by_embedding がexclude_idsを除外する
- [ ] search_articles_by_embedding がmin/max_similarityでフィルタする
- [ ] search_articles_by_unexplored_tags が未探索タグ記事を返す
- [ ] search_articles_by_unexplored_tags のorder_byが動作する
- [ ] search_adjacent_articles が中間類似度の記事を返す
- [ ] 各関数が100ms以内に完了する

## 成果物

- `supabase/migrations/YYYYMMDDHHMMSS_create_search_rpc_functions.sql`

## 依存関係

- 005-02-01: DBスキーマ定義
- EPIC-003: scp_articles テーブル（embedding, tags カラム）
