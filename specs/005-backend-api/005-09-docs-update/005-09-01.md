# Subtask 005-09-01: architecture.md更新

## 概要

.ai/architecture.mdにサーバー主導アーキテクチャを正式化し、ストレージ戦略の変遷を記載する。

## ステータス

- **status**: planned

## ユーザーストーリー

**As a** 開発者
**I want** アーキテクチャドキュメントが最新である
**So that** 正しい設計方針を参照できる

## Acceptance Criteria（EARS記法）

- [ ] WHEN architecture.mdを参照した際
      THEN サーバー主導アーキテクチャの説明が含まれている

- [ ] WHEN ストレージ戦略セクションを参照した際
      THEN IndexedDB → Supabaseへの移行理由が記載されている
      AND IndexedDBはPWAオフライン用として残す旨が記載されている

- [ ] WHEN APIサーバー構成セクションを参照した際
      THEN ドメイン別Colocationパターンの説明が含まれている
      AND 依存フロー（routes → service → repository）が記載されている

## 変更内容

### 追加セクション: ストレージ戦略の変遷

```markdown
## ストレージ戦略

### 変遷

| フェーズ           | ストレージ | 理由                                             |
| ------------------ | ---------- | ------------------------------------------------ |
| EPIC-004（当初）   | IndexedDB  | クライアント側完結、オフライン対応               |
| EPIC-005（変更後） | Supabase   | サーバー主導、複数端末対応、将来のアカウント連携 |

### 現在の構成

- **サーバー側**: SupabasePreferenceStorage（プライマリ）
- **クライアント側**: IndexedDBStorage（PWAオフラインモード用、将来対応）

### 移行理由

1. **複数端末対応**: サーバーに保存することで端末間でデータ共有
2. **アカウント連携**: 将来のSupabase Auth統合に備える
3. **分析基盤**: サーバー側でユーザー行動データを収集
```

### 更新セクション: 責務分離

```markdown
### サーバー主導アーキテクチャ（EPIC-005〜）

| 処理               | クライアント    | サーバー      |
| ------------------ | --------------- | ------------- |
| visitorId生成      | ✅ UUID生成     | -             |
| visitorId保存      | ✅ localStorage | ✅ DB         |
| 嗜好プロファイル   | -               | ✅ 計算・保存 |
| 推薦スコア計算     | -               | ✅            |
| ベクトル検索       | -               | ✅            |
| フィードバック記録 | -               | ✅            |
```

## テストケース

- [ ] サーバー主導アーキテクチャの説明が存在する
- [ ] ストレージ戦略の変遷が記載されている
- [ ] ドメイン別Colocationパターンの説明が存在する
- [ ] 依存フローが正しく記載されている

## 成果物

- `.ai/architecture.md`（更新）

## 依存関係

- なし（並行作業可能）
