# Subtask 010-01-01: article_translations テーブル作成

## 親Story

[010-01: 日本語記事URL管理](./010-01.md)

## ユーザーストーリー

**ペルソナ**: 開発者
**目的**: 多言語記事URLを管理するテーブルを作成する
**価値**: 将来の言語追加時もスキーマ変更不要
**理由**: 拡張性のある設計で運用コストを下げたい

> 開発者として、多言語記事URLを管理するテーブルを作成し、将来の言語追加時もスキーマ変更不要にしたい。なぜなら拡張性のある設計で運用コストを下げたいから。

## Acceptance Criteria

### AC-1: テーブル作成

- [x] WHEN マイグレーションを実行した際
      THEN `article_translations` テーブルが作成される
      AND 以下のカラムが存在する: - `article_id` (TEXT, NOT NULL) - `lang` (TEXT, NOT NULL) - `url` (TEXT, NOT NULL) - `has_translation` (BOOLEAN, DEFAULT NULL) - `checked_at` (TIMESTAMPTZ)

### AC-2: 主キー制約

- [x] WHEN テーブルが作成された際
      THEN `(article_id, lang)` が複合主キーとなる
      AND 同じ article_id と lang の組み合わせは挿入できない

### AC-3: 外部キー制約

- [x] WHEN テーブルが作成された際
      THEN `article_id` が `scp_articles.article_id` への外部キーとなる

### AC-4: インデックス作成

- [x] WHEN マイグレーションを実行した際
      THEN `idx_article_translations_lang` インデックスが作成される
      AND `idx_article_translations_has_translation` 複合インデックスが作成される

## 技術設計

### マイグレーションファイル

```sql
-- supabase/migrations/YYYYMMDDHHMMSS_create_article_translations.sql

-- テーブル作成
CREATE TABLE article_translations (
  article_id TEXT NOT NULL,
  lang TEXT NOT NULL,
  url TEXT NOT NULL,
  has_translation BOOLEAN DEFAULT NULL,
  checked_at TIMESTAMPTZ,
  PRIMARY KEY (article_id, lang),
  CONSTRAINT fk_article_translations_article
    FOREIGN KEY (article_id)
    REFERENCES scp_articles(article_id)
    ON DELETE CASCADE
);

-- コメント
COMMENT ON TABLE article_translations IS '多言語記事URL管理テーブル';
COMMENT ON COLUMN article_translations.article_id IS '記事ID (例: SCP-173)';
COMMENT ON COLUMN article_translations.lang IS '言語コード (例: ja, ko, cn)';
COMMENT ON COLUMN article_translations.url IS '各言語版のURL';
COMMENT ON COLUMN article_translations.has_translation IS '翻訳有無 (NULL=未確認, TRUE=あり, FALSE=なし)';
COMMENT ON COLUMN article_translations.checked_at IS '翻訳確認日時';

-- インデックス
CREATE INDEX idx_article_translations_lang
  ON article_translations(lang);

CREATE INDEX idx_article_translations_has_translation
  ON article_translations(lang, has_translation)
  WHERE has_translation IS NOT FALSE;
```

### RLS（Row Level Security）

```sql
-- 読み取りは全員許可
ALTER TABLE article_translations ENABLE ROW LEVEL SECURITY;

CREATE POLICY "article_translations_select"
  ON article_translations
  FOR SELECT
  USING (true);

-- 更新はサービスロールのみ
CREATE POLICY "article_translations_update"
  ON article_translations
  FOR UPDATE
  USING (auth.role() = 'service_role');
```

## テストケース

- [x] マイグレーション実行後、テーブルが存在する
- [x] 主キー制約が機能する（重複挿入でエラー）
- [x] 外部キー制約が機能する（存在しないarticle_idでエラー）
- [x] インデックスが作成されている
- [x] RLSが有効で、SELECTは許可される

## 依存関係

- **前提**: なし（scp_articlesテーブルは既存）
- **後続**: 010-01-02（初期データ投入）

## 実装状況

- **status**: completed
- **実装ファイル**: `supabase/migrations/20260131000001_create_article_translations.sql`
