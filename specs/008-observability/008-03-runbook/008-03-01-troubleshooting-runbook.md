---
id: "008-03-01"
epic_id: "008"
story_id: "008-03"
epic_title: "運用・監視"
story_title: "運用Runbook整備"
title: "トラブルシューティングRunbook作成"
status: "pending"
created_at: "2026-02-15"
updated_at: "2026-02-15"
completed_at: null
---

# Subtask: トラブルシューティングRunbook作成

## 親Story

[008-03: 運用Runbook整備](./008-03-runbook.md)

## ユーザーストーリー

**ペルソナ**: 開発者・運用者
**目的**: 主要な障害シナリオに対するチェックリスト形式の対応手順を文書化する
**価値**: 障害対応の属人化を防ぎ、一定品質の対応を再現可能にする
**理由**: 障害発生時に冷静に手順を追えるガイドがあると、復旧時間を短縮できる

> 開発者・運用者として、主要な障害シナリオに対するチェックリスト形式の対応手順を文書化したい。なぜなら障害発生時に冷静に手順を追えるガイドがあれば、復旧時間を短縮できるから。

## Acceptance Criteria

- [ ] `docs/runbook.md` ファイルが作成されていること

- [ ] WHEN 「APIがダウンしたとき」セクションを参照した際
      THEN Vercelダッシュボード確認、ヘルスチェック手動実行、デプロイログ確認の手順が記載されている

- [ ] WHEN 「パイプラインが失敗したとき」セクションを参照した際
      THEN GitHub Actionsログ確認、モード確認、手動再実行手順、コスト確認の手順が記載されている

- [ ] WHEN 「DBが応答しないとき」セクションを参照した際
      THEN Supabaseダッシュボード確認、接続情報確認、復旧手順が記載されている

## 設計

### 新規作成ファイル

- `docs/runbook.md`

### Runbook構成

```markdown
# 運用Runbook

## APIがダウンしたとき

- [ ] Vercelダッシュボードでデプロイ状態を確認
- [ ] `curl {API_BASE_URL}/health` でヘルスチェックを手動実行
- [ ] Vercelのデプロイログでエラーを確認
- [ ] 直近のデプロイでリグレッションがないか確認
- [ ] 必要に応じてVercelで前バージョンに戻す

## パイプラインが失敗したとき

- [ ] GitHub Actions実行ログを確認
- [ ] パイプラインモード（full/diff/embedding/tagging）を確認
- [ ] 外部API（OpenAI）のステータスを確認
- [ ] コスト超過で中断された場合はcost_limitを見直す
- [ ] workflow_dispatchで手動再実行

## DBが応答しないとき

- [ ] Supabaseダッシュボードでプロジェクト状態を確認
- [ ] 接続情報（SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY）が正しいか確認
- [ ] Supabaseの無料プラン制限（休止状態等）に該当しないか確認
- [ ] `curl {API_BASE_URL}/health` で degraded が返るか確認
```

### 技術的考慮事項

- チェックリスト形式で、上から順に実行すれば対応できる構成
- 外部URLへの直接リンクは含めない（URLが変わる可能性があるため、ダッシュボード名で記述）
- コード実装は伴わないドキュメントのみのタスク

## テストケース

```markdown
# レビュー確認項目

# 1. docs/runbook.md が存在すること

# 2. 「APIがダウンしたとき」セクションが存在し、手順が記載されていること

# 3. 「パイプラインが失敗したとき」セクションが存在し、手順が記載されていること

# 4. 「DBが応答しないとき」セクションが存在し、手順が記載されていること

# 5. 各セクションがチェックリスト形式であること
```

## 完了確認

- 確認日:
- 確認者:
- 備考:

## 参照ドキュメント

- [ワークフロー定義](../../../.ai/WORKFLOW.md)
