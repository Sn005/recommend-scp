# Subtask 004-04-01: Like/Dislikeフィードバック処理

## 概要

ユーザーのLike/Dislikeフィードバックを記録し、嗜好プロファイルを更新する機能を実装する。

## ステータス

- **status**: completed

## ユーザーストーリー

**As a** SCPファン
**I want** 記事をLike/Dislikeで評価したい
**So that** 自分の好みに合った推薦を受けられる

## Acceptance Criteria（EARS記法）

- [x] WHEN ユーザーが記事をLikeした際
      THEN Feedbackレコードがストレージに保存される
      AND その記事のタグに対する重みが +1.0 増加する
      AND プロファイルが再計算・保存される

- [x] WHEN ユーザーが記事をDislikeした際
      THEN Feedbackレコードがストレージに保存される
      AND その記事がgetDislikedArticleIdsの結果に含まれる
      AND タグ重みは変化しない

- [x] WHEN 同じ記事に対して再度フィードバックした際
      THEN 既存のフィードバックが上書きされる

- [x] WHEN フィードバックを記録する際
      THEN 記事のタグ情報をSupabaseから取得する
      AND tagWeightsに反映する

## 技術設計

### シグナルプロセッサ

```typescript
// packages/shared/src/recommendation/signal-processor.ts

export class SignalProcessor {
  constructor(
    private storage: PreferenceStorage,
    private profiler: PreferenceProfiler
  ) {}

  /**
   * フィードバックを記録
   * - Like/Dislikeを統一的に処理（DRY原則）
   * - 同じ記事への再フィードバックは上書き
   */
  async recordFeedback(
    visitorId: string,
    articleId: string,
    type: "like" | "dislike"
  ): Promise<PreferenceProfile> {
    // バリデーション
    if (!visitorId) throw new Error("visitorId is required");
    if (!articleId) throw new Error("articleId is required");

    // フィードバック保存
    await this.storage.addFeedback({
      id: `${visitorId}_${articleId}`,
      visitorId,
      articleId,
      type,
      createdAt: new Date().toISOString(),
    });

    // プロファイル再計算・保存
    const profile = await this.profiler.recalculateProfile(visitorId);
    await this.storage.saveProfile(profile);

    return profile;
  }
}
```

### タグ重み更新ロジック

```typescript
// PreferenceProfiler.recalculateProfile 内

const likedArticles = feedback.filter((f) => f.type === "like");

for (const like of likedArticles) {
  const articleTags = await this.getArticleTags(like.articleId);
  for (const tag of articleTags) {
    tagWeights[tag] = (tagWeights[tag] ?? 0) + 1.0;
  }
}
```

## テストケース

- [x] Likeでフィードバックが保存される
- [x] Likeで対象記事のタグ重みが+1.0増加する
- [x] Likeでプロファイルが再計算される
- [x] Dislikeでフィードバックが保存される
- [x] Dislikeでタグ重みは変化しない
- [x] Dislike済み記事がgetDislikedArticleIdsに含まれる
- [x] 同じ記事への再フィードバックで上書きされる

## 実装状況

- **実装日**: 2026-01-22
- **実装ファイル**:
  - `packages/shared/src/recommendation/signal-processor.ts`
  - `packages/shared/src/recommendation/__tests__/signal-processor.test.ts`
- **テスト件数**: 18件（全パス）

## 成果物

- `packages/shared/src/recommendation/signal-processor.ts`

## 依存関係

- 004-01-03: 嗜好プロファイル計算
