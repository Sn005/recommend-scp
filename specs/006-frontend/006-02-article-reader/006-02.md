# Story: 記事閲覧

## 親EPIC

[006: フロントエンドUI](../006-frontend.md)

## ユーザーストーリー

**ペルソナ**: SCPファン
**目的**: 推薦された記事を没入感のある全画面で読む
**価値**: 気に入った記事を保存し、興味のない記事はスキップできる
**理由**: 手軽にSCP記事を楽しみたい

> SCPファンとして、推薦された記事を没入感のある全画面で読み、気に入った記事を保存したい。なぜなら手軽にSCP記事を楽しみたいから。

## 背景

### コア体験

- **没入型閲覧**: 全画面WebViewで元サイトの記事をそのまま表示
- **シームレス遷移**: 記事最下部で次の記事が自動読み込み（無限スクロール）
- **ミニマル操作**: ボタンは最小限（お気に入り、次へ）

### UIレイアウト（モックアップ準拠）

```
┌─────────────────────────────────────┐
│ ☰                                   │ ← メニューボタン（左上固定）
│                                     │
│                                     │
│        [全画面 WebView]             │
│        （元サイトの記事）           │
│                                     │
│        （スクロール中...）          │
│                                     │
│        ─── シームレス ───           │
│                                     │
│        [次の記事]                   │
│        ↑ 自動読み込み               │
│                                     │
│       ══════════════════            │ ← プログレスバー
│      ┌─────────────────┐            │
│      │    ♡       →    │            │ ← フローティングピルナビ
│      └─────────────────┘            │
└─────────────────────────────────────┘
```

**デザインポイント:**

- ヘッダーは廃止（☰メニューボタンは共通UIで提供）
- ボトムにフローティングピルナビ（♡ お気に入り、→ 次へ）
- プログレスバーで推薦の進捗を表示
- 戻るボタンは廃止（ドロワーメニューで統一）

### フィードバック判定ロジック

| アクション             | フィードバック種別 | 重み |
| ---------------------- | ------------------ | ---- |
| 記事最下部到達（読了） | 暗黙的Like         | 中   |
| 「次へ」ボタン押下     | Dislike            | 中   |
| お気に入りボタン押下   | 明示的Like         | 高   |

## Acceptance Criteria

### AC-1: 記事閲覧画面表示

- [x] WHEN ユーザーがオンボーディング完了後に遷移した際
      THEN 記事閲覧画面が表示される
      AND POST /recommend APIから推薦記事を取得する
      AND 最初の記事がWebViewで表示される

### AC-2: 全画面WebView

- [x] WHEN 記事が表示された際
      THEN 画面全体にWebView（iframe）が表示される
      AND 元サイトの記事コンテンツがそのまま表示される
      AND 記事内のスクロール・リンク押下が自由にできる

### AC-3: 無限スクロール（次記事読み込み）

- [x] WHEN ユーザーが現在の記事の最下部に到達した際
      THEN 次の推薦記事が自動的に読み込まれる
      AND 記事間にシームレスに接続される
      AND ローディングインジケータが一時的に表示される

### AC-4: 読了判定（暗黙的Like）

- [x] WHEN ユーザーが記事最下部に到達した際
      THEN POST /feedback APIで暗黙的Likeが記録される
      AND 同じ記事は再度記録されない（重複防止）

### AC-5: スキップ（Dislike）

- [x] WHEN ユーザーが「次へ」ボタン（ピルナビの→）をタップした際
      THEN POST /feedback APIでDislikeが記録される
      AND 次の推薦記事が表示される
      AND スクロール位置が先頭にリセットされる

### AC-6: お気に入り追加

- [x] WHEN ユーザーがお気に入りボタン（ピルナビの♡）をタップした際
      GIVEN 記事がお気に入りに未追加の場合
      THEN POST /feedback APIで明示的Likeが記録される
      AND ボタンの状態が「追加済み」に変化する
      AND ポップアニメーションが再生される

### AC-7: お気に入り解除

- [x] WHEN ユーザーがお気に入りボタン（ピルナビの♡）をタップした際
      GIVEN 記事が既にお気に入りの場合
      THEN お気に入りが解除される
      AND ボタンの状態が「未追加」に変化する

### AC-8: 推薦切れ対応

- [x] WHEN POST /recommend APIが空の結果を返した際
      THEN 「すべての推薦を読みました」メッセージが表示される
      AND 再オンボーディングを提案するボタンが表示される

### AC-9: エラーハンドリング

- [x] WHEN 記事読み込み中にエラーが発生した際
      THEN シンプルなエラーメッセージが表示される
      AND リトライボタンが表示される

## 関連Subtask

| ID                          | 名前                  | 概要                                    | ステータス |
| --------------------------- | --------------------- | --------------------------------------- | ---------- |
| [006-02-01](./006-02-01.md) | 記事閲覧ページ実装    | ページコンポーネント・レイアウト        | completed  |
| [006-02-02](./006-02-02.md) | WebViewコンポーネント | iframe表示・スクロール連携              | completed  |
| [006-02-03](./006-02-03.md) | フローティングUI制御  | スクロール検知・PillNav/ProgressBar表示 | completed  |
| [006-02-04](./006-02-04.md) | 無限スクロールフック  | 次記事読み込み・Intersection Observer   | completed  |
| [006-02-05](./006-02-05.md) | フィードバック記録    | Like/Dislike/Favorite API連携           | completed  |
| [006-02-06](./006-02-06.md) | お気に入りボタン      | 状態管理・トグル処理                    | completed  |

## 技術設計

### ディレクトリ構成（コロケーション）

```
app/(main)/recommend/
├── page.tsx
├── _components/
│   ├── ArticleWebView/
│   │   ├── index.tsx
│   │   └── useArticleWebView.ts
│   └── RecommendContent/
│       └── index.tsx
└── _hooks/
    ├── useScrollPosition.ts
    ├── useInfiniteArticles.ts
    └── useReadingProgress.ts
```

**共通UIコンポーネント（shared/から使用）:**

- `Drawer` - ドロワーメニュー
- `MenuButton` - 左上☰ボタン
- `PillNav` - フローティングピルナビ
- `ProgressBar` - 進捗バー
- `Badge` - オブジェクトクラスバッジ

### スクロール位置検知（Intersection Observer）

```typescript
// 各記事の境界を検知してタイトルを更新
const observer = new IntersectionObserver(
  (entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const articleId = entry.target.dataset.articleId;
        setCurrentArticle(articleId);
      }
    });
  },
  { threshold: 0.5 }
);
```

### API連携

```typescript
// 推薦取得
const articles = await api.recommend.$post({
  json: { visitorId, count: 5 },
});

// フィードバック記録
await api.feedback.$post({
  json: {
    visitorId,
    articleId,
    type: "like" | "dislike" | "favorite",
  },
});
```

## デザイン準拠チェックリスト

**参照モック**: `mockups/header-6-minimal-2btn.html`

### 全体レイアウト

- [x] 全画面WebView（iframe）
- [x] ヘッダーは廃止（メニューボタンのみ）
- [x] フローティングピルナビ（画面下部中央）
- [x] プログレスバー（ピルナビ上部）

### メニューボタン（左上）

- [x] 丸型（w-10 h-10 rounded-full）
- [x] Glassmorphism（背景blur + 半透明）
- [x] SVGハンバーガーアイコン（☰テキスト禁止）
- [x] box-shadow: 0 2px 8px rgba(0,0,0,0.08)

### フローティングピルナビ

- [x] 中央配置（left: 50% + transform: translateX(-50%)）
- [x] ピル型（rounded-full、約100px幅）
- [x] Glassmorphism（rgba(255,255,255,0.3) + blur(20px)）
- [x] box-shadow: 0 4px 24px rgba(0,0,0,0.12)
- [x] 2ボタン構成（♡お気に入り、→次へ）
- [x] SVGアイコン使用（テキスト絵文字禁止）

### プログレスバー

- [x] height: 2px
- [x] 背景: rgba(0,0,0,0.05)
- [x] fill: primary色
- [x] margin: 0 16px

### お気に入りボタン

- [x] SVGハートアイコン（アウトライン）
- [x] お気に入り時: 塗りつぶし + 赤色（#EF4444）
- [x] ポップアニメーション（heartPop）

## 備考

- WebView（iframe）はSCP Wikiのクロスオリジン制約に注意
- 読了判定の閾値は調整可能（スクロール率 90% など）
- 将来的にエッジスワイプによるスキップを検討
- **実装前に必ず `mockups/header-6-minimal-2btn.html` を確認すること**
