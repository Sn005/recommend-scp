# Subtask 006-00-05: UIコンポーネント基盤

## 概要

shadcn/uiを導入し、アクセシブルで再利用可能なUIコンポーネント基盤を構築する。
cn関数やLucide iconsなど、UIユーティリティも整備する。

## ステータス

- **status**: planned

## 親Story

[006-00: フロントエンド環境構築](./006-00.md)

## ユーザーストーリー

**ペルソナ**: フロントエンド開発者
**目的**: アクセシブルで一貫性のあるUIコンポーネントを使用する
**価値**: UI実装を高速化し、アクセシビリティを担保できる
**理由**: ゼロからコンポーネントを作る時間を節約したい

> フロントエンド開発者として、アクセシブルで一貫性のあるUIコンポーネントを使用して、UI実装を高速化したい。なぜならゼロからコンポーネントを作る時間を節約し、アクセシビリティを担保したいから。

## Acceptance Criteria（EARS記法）

### AC-1: shadcn/ui初期化

- [ ] WHEN `components.json` を確認した際
      THEN shadcn/uiの設定が存在する

### AC-2: cn関数

- [ ] WHEN `cn()` 関数を使用した際
      THEN Tailwindクラスが適切にマージされる
      AND 競合するクラスは後のものが優先される

### AC-3: Buttonコンポーネント

- [ ] WHEN `<Button>` コンポーネントを使用した際
      THEN variant（default, outline, ghost等）が適用される
      AND size（default, sm, lg, icon）が適用される

### AC-4: Lucide icons

- [ ] WHEN Lucide iconsをインポートした際
      THEN アイコンが正しくレンダリングされる

### AC-5: アクセシビリティ

- [ ] WHEN shadcn/uiコンポーネントを使用した際
      THEN 適切なaria属性が設定される
      AND キーボード操作が可能である

### AC-6: ダークモード対応

- [ ] WHEN ダークモードを有効にした際
      THEN shadcn/uiコンポーネントのスタイルが切り替わる

## 技術設計

### 依存パッケージ

```json
{
  "dependencies": {
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.5.0",
    "lucide-react": "^0.460.0",
    "@radix-ui/react-slot": "^1.1.0"
  }
}
```

### components.json

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/shared/components",
    "utils": "@/shared/lib/utils",
    "ui": "@/shared/components/ui",
    "lib": "@/shared/lib",
    "hooks": "@/shared/hooks"
  }
}
```

### src/shared/lib/utils.ts

```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

/**
 * Tailwindクラスをマージするユーティリティ
 * clsxで条件付きクラスを結合し、tailwind-mergeで競合を解決
 *
 * @example
 * cn("px-2 py-1", "px-4") // => "py-1 px-4"
 * cn("text-red-500", isActive && "text-blue-500")
 */
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

### Button コンポーネント

```tsx
// src/shared/components/ui/Button/index.tsx
import * as React from "react";
import { Slot } from "@radix-ui/react-slot";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/shared/lib/utils";

const buttonVariants = cva(
  "inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50",
  {
    variants: {
      variant: {
        default: "bg-scp-accent text-white shadow hover:bg-scp-accent/90",
        destructive: "bg-destructive text-destructive-foreground shadow-sm hover:bg-destructive/90",
        outline:
          "border border-input bg-background shadow-sm hover:bg-accent hover:text-accent-foreground",
        secondary: "bg-secondary text-secondary-foreground shadow-sm hover:bg-secondary/80",
        ghost: "hover:bg-accent hover:text-accent-foreground",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default: "h-9 px-4 py-2",
        sm: "h-8 rounded-md px-3 text-xs",
        lg: "h-10 rounded-md px-8",
        icon: "h-9 w-9",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
);

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<typeof buttonVariants> {
  asChild?: boolean;
}

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    const Comp = asChild ? Slot : "button";
    return (
      <Comp className={cn(buttonVariants({ variant, size, className }))} ref={ref} {...props} />
    );
  }
);
Button.displayName = "Button";

export { Button, buttonVariants };
```

### globals.css への追加（CSS変数）

```css
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 0 0% 3.9%;
    --card: 0 0% 100%;
    --card-foreground: 0 0% 3.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 0 0% 3.9%;
    --primary: 0 0% 9%;
    --primary-foreground: 0 0% 98%;
    --secondary: 0 0% 96.1%;
    --secondary-foreground: 0 0% 9%;
    --muted: 0 0% 96.1%;
    --muted-foreground: 0 0% 45.1%;
    --accent: 0 0% 96.1%;
    --accent-foreground: 0 0% 9%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 89.8%;
    --input: 0 0% 89.8%;
    --ring: 0 0% 3.9%;
    --radius: 0.5rem;
  }

  .dark {
    --background: 0 0% 3.9%;
    --foreground: 0 0% 98%;
    --card: 0 0% 3.9%;
    --card-foreground: 0 0% 98%;
    --popover: 0 0% 3.9%;
    --popover-foreground: 0 0% 98%;
    --primary: 0 0% 98%;
    --primary-foreground: 0 0% 9%;
    --secondary: 0 0% 14.9%;
    --secondary-foreground: 0 0% 98%;
    --muted: 0 0% 14.9%;
    --muted-foreground: 0 0% 63.9%;
    --accent: 0 0% 14.9%;
    --accent-foreground: 0 0% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 14.9%;
    --input: 0 0% 14.9%;
    --ring: 0 0% 83.1%;
  }
}
```

### tailwind.config.ts 追加設定

```typescript
// theme.extend に追加
{
  borderRadius: {
    lg: "var(--radius)",
    md: "calc(var(--radius) - 2px)",
    sm: "calc(var(--radius) - 4px)",
  },
  colors: {
    border: "hsl(var(--border))",
    input: "hsl(var(--input))",
    ring: "hsl(var(--ring))",
    background: "hsl(var(--background))",
    foreground: "hsl(var(--foreground))",
    primary: {
      DEFAULT: "hsl(var(--primary))",
      foreground: "hsl(var(--primary-foreground))",
    },
    secondary: {
      DEFAULT: "hsl(var(--secondary))",
      foreground: "hsl(var(--secondary-foreground))",
    },
    destructive: {
      DEFAULT: "hsl(var(--destructive))",
      foreground: "hsl(var(--destructive-foreground))",
    },
    muted: {
      DEFAULT: "hsl(var(--muted))",
      foreground: "hsl(var(--muted-foreground))",
    },
    accent: {
      DEFAULT: "hsl(var(--accent))",
      foreground: "hsl(var(--accent-foreground))",
    },
    popover: {
      DEFAULT: "hsl(var(--popover))",
      foreground: "hsl(var(--popover-foreground))",
    },
    card: {
      DEFAULT: "hsl(var(--card))",
      foreground: "hsl(var(--card-foreground))",
    },
  },
}
```

### ファイル構成

```
apps/web/src/shared/
├── components/
│   └── ui/
│       ├── Button/
│       │   ├── index.tsx
│       │   └── Button.test.tsx
│       └── index.ts         # re-export
├── lib/
│   └── utils.ts             # cn関数
└── hooks/
    └── index.ts
```

### 使用例

```tsx
import { Button } from "@/shared/components/ui/Button";
import { Heart, ArrowRight } from "lucide-react";

export function Example() {
  return (
    <div className="flex gap-2">
      <Button>デフォルト</Button>
      <Button variant="outline">アウトライン</Button>
      <Button variant="ghost">ゴースト</Button>
      <Button size="icon">
        <Heart className="h-4 w-4" />
      </Button>
      <Button>
        次へ <ArrowRight className="ml-2 h-4 w-4" />
      </Button>
    </div>
  );
}
```

## 追加するコンポーネント（初期）

| コンポーネント | 用途             | 006-01以降で使用 |
| -------------- | ---------------- | ---------------- |
| Button         | 各種ボタン       | ✅               |
| Card           | パック選択カード | ✅               |
| Input          | SCP番号入力      | ✅               |
| Skeleton       | ローディング     | ✅               |

※ 必要に応じて `npx shadcn@latest add [component]` で追加

## テストケース

- [ ] `cn("px-2", "px-4")` が `"px-4"` を返す
- [ ] `cn("text-red-500", false && "text-blue-500")` が `"text-red-500"` を返す
- [ ] Buttonコンポーネントがレンダリングされる
- [ ] Button variant="outline" でアウトラインスタイルが適用される
- [ ] Button size="sm" で小さいサイズが適用される
- [ ] Lucide iconsがレンダリングされる
- [ ] Buttonにフォーカスできる（キーボード操作）
- [ ] ダークモードでスタイルが切り替わる

## 成果物

- `apps/web/components.json`
- `apps/web/src/shared/lib/utils.ts`
- `apps/web/src/shared/components/ui/Button/index.tsx`
- `apps/web/src/app/globals.css` 更新（CSS変数追加）
- `apps/web/tailwind.config.ts` 更新（shadcn/ui設定）
- `apps/web/package.json` 更新（依存追加）

## 依存関係

- 006-00-01: Next.jsプロジェクト初期化
- 006-00-02: Tailwind CSS設定

## 備考

- shadcn/uiはコピー&ペースト方式（node_modulesにインストールされない）
- 必要なコンポーネントのみを追加してバンドルサイズを最適化
- Radix UIベースでアクセシビリティを担保
- カスタマイズはコンポーネントファイルを直接編集
