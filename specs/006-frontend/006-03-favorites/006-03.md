# Story: お気に入り管理

## 親EPIC

[006: フロントエンドUI](../006-frontend.md)

## ユーザーストーリー

**ペルソナ**: ユーザー
**目的**: 保存した記事を一覧で確認し、いつでも読み返す
**価値**: 気に入った記事にすぐアクセスできる
**理由**: 後で読み返したい記事を整理したい

> ユーザーとして、保存した記事を一覧で確認し、いつでも読み返したい。なぜなら後で読み返したい記事を整理したいから。

## 背景

### MVP機能

- 一覧表示
- タップで再読
- お気に入り解除

### 将来対応（MVP外）

- 並び替え（追加順 / タイトル順）
- フォルダ/タグ分類
- 外部共有

### UIレイアウト（モックアップ準拠）

```
┌─────────────────────────────────────┐
│ ☰                                   │ ← メニューボタン（左上固定）
│     お気に入り              12件    │ ← スクロールで消える
│                                     │
│  ┌─────────────────────────────┐   │
│  │ [Safe] +574                  │   │
│  │ SCP-5000                     │   │
│  │ Why? - 財団が人類との敵対を... → │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ [Keter] +1024               │   │
│  │ SCP-173                      │   │
│  │ 彫刻 - オリジナル            → │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ [Safe] +2048                │   │
│  │ SCP-999                      │   │
│  │ くすぐりお化け              → │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

**デザインポイント:**

- 戻るボタンは廃止（ドロワーメニューで統一）
- ☰メニューボタンは共通UIで提供
- カードにオブジェクトクラスバッジとレーティングを表示
- タップで記事詳細へ遷移

## Acceptance Criteria

### AC-1: お気に入り一覧表示

- [ ] WHEN ユーザーがお気に入り画面を開いた際
      THEN GET /favorites APIからお気に入り一覧を取得する
      AND 記事がリスト形式で表示される
      AND 各記事のタイトル・概要が表示される

### AC-2: 記事の再読

- [ ] WHEN ユーザーがお気に入り一覧の記事をタップした際
      THEN 記事閲覧画面に遷移する
      AND 選択した記事がWebViewで表示される

### AC-3: お気に入り解除

- [ ] WHEN ユーザーが解除ボタン（×）をタップした際
      THEN DELETE /favorites APIが呼び出される
      AND 一覧から該当記事が削除される
      AND アニメーション付きで消える

### AC-4: 空状態

- [ ] WHEN お気に入りが0件の場合
      THEN 「まだお気に入りがありません」メッセージが表示される
      AND 「記事を探す」ボタンが表示される

### AC-5: ナビゲーション

- [ ] WHEN ユーザーがドロワーメニューを開いた際
      THEN 他の画面（推薦、履歴）に遷移できる
      AND 現在のページ（お気に入り）がハイライトされる

### AC-6: エラーハンドリング

- [ ] WHEN お気に入り一覧の取得に失敗した際
      THEN シンプルなエラーメッセージが表示される
      AND リトライボタンが表示される

## 関連Subtask

| ID                          | 名前               | 概要                             | ステータス |
| --------------------------- | ------------------ | -------------------------------- | ---------- |
| [006-03-01](./006-03-01.md) | お気に入り画面実装 | ページコンポーネント・レイアウト | planned    |
| [006-03-02](./006-03-02.md) | お気に入りリスト   | 一覧表示・削除処理               | planned    |
| [006-03-03](./006-03-03.md) | API連携フック      | GET/DELETE /favorites            | planned    |

## 技術設計

### ディレクトリ構成（コロケーション）

```
app/(main)/favorites/
├── page.tsx
├── _components/
│   ├── FavoriteList/
│   │   ├── index.tsx
│   │   └── useFavoriteList.ts
│   └── FavoriteCard/
│       └── index.tsx
└── _hooks/
    └── useFavorites.ts
```

**共通UIコンポーネント（shared/から使用）:**

- `Drawer` - ドロワーメニュー
- `MenuButton` - 左上☰ボタン
- `Badge` - オブジェクトクラスバッジ

### API連携

```typescript
// お気に入り一覧取得
const favorites = await api.favorites.$get({
  query: { visitorId },
});

// お気に入り削除
await api.favorites[":id"].$delete({
  param: { id: articleId },
});
```

## デザイン準拠チェックリスト

**参照モック**: `mockups/favorites-v2.html`

### カードデザイン

- [ ] カードはwhite背景 + border-radius: 16px
- [ ] box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04)
- [ ] padding: 16px
- [ ] オブジェクトクラスバッジは左上に配置（Safe/Euclid/Keter色）
- [ ] 評価（+574等）はバッジ右横に表示
- [ ] タイトルはfont-semibold text-gray-800
- [ ] 概要はtext-sm text-gray-500、1行で省略（truncate）
- [ ] 右端に矢印アイコン（`<Icon name="chevron-right" />`）

### レイアウト

- [ ] ヘッダーにタイトル「お気に入り」+ 件数表示
- [ ] 左上にMenuButton（共通UI使用）
- [ ] カード間のマージンは12px（space-y-3）
- [ ] 左右パディングは16px（px-4）

### 空状態

- [ ] 中央配置のメッセージ
- [ ] 「記事を探す」ボタンはprimary色

### 削除UI

- [ ] スワイプ削除 または ×ボタン（モック参照）
- [ ] 削除時のフェードアウトアニメーション

## 備考

- MVP後に並び替え・フォルダ分類を追加予定
- 外部共有機能も将来対応
- **実装前に必ず `mockups/favorites-v2.html` を確認すること**
