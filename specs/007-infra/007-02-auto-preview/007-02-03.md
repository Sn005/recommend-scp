# Subtask 007-02-03: プレビュー認証

## 概要

Vercelプレビュー環境にベーシック認証を実装し、特定の人間のみアクセス可能にする。

## ユーザーストーリー

```
AS A 開発者
I WANT プレビュー環境に認証をかけたい
SO THAT 開発中の画面が第三者に見られないようにする
```

## Acceptance Criteria

### AC1: Middleware実装

- [x] WHEN `apps/web/middleware.ts` を確認した際
      THEN ベーシック認証ロジックが実装されている
- [x] WHEN Middlewareを確認した際
      THEN `VERCEL_ENV === "preview"` の場合のみ認証が有効になる
- [x] WHEN Middlewareを確認した際
      THEN 静的アセット（`_next/static`, `favicon.ico`）は認証対象外になっている

### AC2: 認証動作（プレビュー環境）

- [x] WHEN プレビューURLに認証なしでアクセスした際
      THEN HTTP 401 が返却される
- [x] WHEN 401レスポンスを確認した際
      THEN `WWW-Authenticate: Basic realm="Preview"` ヘッダが含まれる
- [x] WHEN ブラウザでアクセスした際
      THEN 認証ダイアログが表示される
- [x] WHEN 正しい認証情報を入力した際
      THEN ページが表示される
- [x] WHEN 誤った認証情報を入力した際
      THEN HTTP 401 が返却される

### AC3: 認証無効（ローカル・本番）

- [x] WHEN ローカル環境（`VERCEL_ENV` 未設定）でアクセスした際
      THEN 認証なしでページが表示される
- [x] WHEN 本番環境（`VERCEL_ENV === "production"`）でアクセスした際
      THEN 認証なしでページが表示される

### AC4: 環境変数

- [ ] WHEN Vercel Dashboard を確認した際
      THEN `PREVIEW_AUTH_USER` が Preview scope で設定されている
- [ ] WHEN Vercel Dashboard を確認した際
      THEN `PREVIEW_AUTH_PASS` が Preview scope で設定されている
- [x] WHEN 環境変数が未設定の場合
      THEN 認証がスキップされる（フォールバック動作）

### AC5: API Routeの保護

- [x] WHEN プレビュー環境で `/api/health` に認証なしでアクセスした際
      THEN HTTP 401 が返却される
- [x] WHEN 認証成功後に `/api/health` にアクセスした際
      THEN 正常なレスポンスが返却される

## 技術仕様

### 新規ファイル: `apps/web/middleware.ts`

```typescript
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export function middleware(request: NextRequest) {
  // プレビュー環境のみ認証を適用
  // eslint-disable-next-line n/no-process-env
  if (process.env.VERCEL_ENV !== "preview") {
    return NextResponse.next();
  }

  // 環境変数が未設定の場合はスキップ（安全側に倒す）
  // eslint-disable-next-line n/no-process-env
  const expectedUser = process.env.PREVIEW_AUTH_USER;
  // eslint-disable-next-line n/no-process-env
  const expectedPass = process.env.PREVIEW_AUTH_PASS;

  if (!expectedUser || !expectedPass) {
    return NextResponse.next();
  }

  // Authorization ヘッダを確認
  const authHeader = request.headers.get("authorization");

  if (!authHeader || !authHeader.startsWith("Basic ")) {
    return new NextResponse("Authentication required", {
      status: 401,
      headers: {
        "WWW-Authenticate": 'Basic realm="Preview"',
      },
    });
  }

  // Basic認証のデコード
  try {
    const base64Credentials = authHeader.split(" ")[1];
    const credentials = Buffer.from(base64Credentials, "base64").toString("utf-8");
    const [user, pass] = credentials.split(":");

    if (user === expectedUser && pass === expectedPass) {
      return NextResponse.next();
    }
  } catch {
    // デコード失敗
  }

  return new NextResponse("Invalid credentials", {
    status: 401,
    headers: {
      "WWW-Authenticate": 'Basic realm="Preview"',
    },
  });
}

export const config = {
  matcher: [
    /*
     * 以下を除く全てのパスにマッチ:
     * - _next/static (静的ファイル)
     * - _next/image (画像最適化)
     * - favicon.ico (ファビコン)
     */
    "/((?!_next/static|_next/image|favicon.ico).*)",
  ],
};
```

### Vercel環境変数設定

| 変数名              | Scope   | 値         | 説明                                 |
| ------------------- | ------- | ---------- | ------------------------------------ |
| `PREVIEW_AUTH_USER` | Preview | `preview`  | 認証ユーザー名                       |
| `PREVIEW_AUTH_PASS` | Preview | `<random>` | 認証パスワード（ランダム文字列推奨） |

**Scope設定の重要性:**

- `Preview` scope のみに設定することで、本番環境には適用されない
- Vercelが自動的に `VERCEL_ENV` を設定する

### パスワード生成例

```bash
# ランダムな32文字のパスワードを生成
openssl rand -base64 24
```

## 成果物

| ファイル                 | 説明                     |
| ------------------------ | ------------------------ |
| `apps/web/middleware.ts` | ベーシック認証Middleware |

## テストケース

### ローカル動作確認

- [x] `pnpm dev` で起動後、認証なしでアクセスできる
- [x] `VERCEL_ENV=preview` を設定しても、環境変数未設定なら認証スキップ

### Vercelプレビュー確認

- [ ] プレビューURLにアクセスすると認証ダイアログが表示される
- [ ] 正しい認証情報でログインできる
- [ ] 誤った認証情報では弾かれる
- [ ] 認証後、ページ遷移しても再認証不要（セッション維持）

### Vercel本番確認

- [ ] 本番URLには認証なしでアクセスできる

## 依存関係

- 007-02-01（Vercel連携設定）が完了していること

## セキュリティ考慮事項

- Basic認証はHTTPS上でのみ安全（Vercelは強制HTTPS）
- パスワードは十分な強度（32文字以上推奨）
- 本番環境には絶対に適用しない（`VERCEL_ENV` チェック）

## 実装状況

- **status**: completed（コード作業完了、手動設定待ち）
- **作成日**: 2026-01-31
- **更新日**: 2026-01-31

### 完了した作業

1. `apps/web/middleware.ts` - ベーシック認証Middleware実装
2. `apps/web/src/test/middleware.test.ts` - 全16テストケースがパス

### 残りの手動作業

以下は `docs/vercel-setup.md` の手順に従って実施してください：

1. Vercel Dashboard で `PREVIEW_AUTH_USER` を Preview scope で設定
2. Vercel Dashboard で `PREVIEW_AUTH_PASS` を Preview scope で設定
3. プレビュー環境での認証動作確認
