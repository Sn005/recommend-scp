# Subtask 007-02-02: Hono API統合

## 概要

既存のHono APIをNext.js API Routesにマウントし、Vercelで動作させる。

> **Note**: 当初Edge Runtimeを予定していたが、api-serverの依存（pino, find-up等）が
> Node.js固有のため、現時点ではNode.js Runtimeを使用。Edge Runtime対応は別Subtaskで対応予定。

## ユーザーストーリー

```
AS A フロントエンド開発者
I WANT Vercelプレビューで `/api/*` にアクセスするとHono APIが応答する
SO THAT 単一URLでフロントエンドとAPIの両方を確認できる
```

## 背景

現在の構成:

- `apps/api-server/src/app.ts` で `createApp(supabase)` がHonoアプリを生成
- `apps/web/src/shared/lib/api-client.ts` で `NEXT_PUBLIC_API_URL` を使用

ハイブリッド方式:

- ローカル: `NEXT_PUBLIC_API_URL=http://localhost:3001`（api-server直接）
- Vercel: `NEXT_PUBLIC_API_URL=/api`（Next.js API Routes経由）

## Acceptance Criteria

### AC1: API Routeファイル作成

- [x] WHEN `apps/web/src/app/api/[...route]/route.ts` を確認した際
      THEN Hono appがマウントされている
- [x] WHEN ファイルを確認した際
      THEN `export const runtime = "nodejs"` が設定されている
      （Edge Runtimeはapi-serverの依存がNode.js固有のため保留）
- [x] WHEN ファイルを確認した際
      THEN GET/POST/PUT/DELETE/PATCH ハンドラがエクスポートされている

### AC2: Hono統合

- [x] WHEN `hono/vercel` パッケージを確認した際
      THEN `hono` パッケージに含まれている（別パッケージ不要）
- [x] WHEN API Routeを確認した際
      THEN `handle(app)` で Hono app がラップされている
- [x] WHEN API Routeを確認した際
      THEN Supabase クライアントが正しく初期化されている

### AC3: ローカル開発の互換性

- [x] WHEN ローカルで `pnpm dev` を実行した際
      THEN `apps/web` が localhost:3000 で起動する
- [x] WHEN ローカルで `pnpm dev` を実行した際
      THEN `apps/api-server` が localhost:3001 で起動する
- [x] WHEN ローカルのフロントエンドからAPIを呼び出した際
      THEN localhost:3001 のapi-serverに接続される（NEXT_PUBLIC_API_URL設定による）

### AC4: Vercel環境での動作

- [x] WHEN Vercelプレビューで `/api/health` にアクセスした際
      THEN `{ status: "ok" }` が返却される
- [x] WHEN Vercelプレビューで `/api/onboarding/packs` にアクセスした際
      THEN オンボーディングパック一覧が返却される
- [x] WHEN フロントエンドからAPIを呼び出した際
      THEN 相対パス `/api/*` で正常に通信できる

### AC5: 型安全性の維持

- [x] WHEN `api-client.ts` を確認した際
      THEN 既存のHono RPCクライアントがそのまま動作する
- [x] WHEN APIレスポンスの型を確認した際
      THEN TypeScriptの型推論が正しく機能する

## 技術仕様

### 新規ファイル: `apps/web/src/app/api/[...route]/route.ts`

```typescript
import { handle } from "hono/vercel";
import { createApp } from "@recommend-scp/api-server/app";
import { createClient } from "@supabase/supabase-js";

// Supabase クライアント初期化
const getSupabaseClient = () => {
  const supabaseUrl = process.env.SUPABASE_URL;
  const supabaseServiceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;
  if (!supabaseUrl || !supabaseServiceRoleKey) {
    throw new Error("Required Supabase environment variables are not set");
  }
  return createClient(supabaseUrl, supabaseServiceRoleKey, {
    auth: { autoRefreshToken: false, persistSession: false },
  });
};

// Hono app 生成
const supabase = getSupabaseClient();
const app = createApp(supabase);

// Vercel Handler
export const GET = handle(app);
export const POST = handle(app);
export const PUT = handle(app);
export const DELETE = handle(app);
export const PATCH = handle(app);

// Node.js Runtime（api-serverの依存がNode.js固有のため）
export const runtime = "nodejs";
```

### パッケージ追加

```bash
# hono/vercel は hono パッケージに含まれている（別パッケージ不要）
# api-server と supabase-js を追加
pnpm --filter web add @supabase/supabase-js
```

### api-serverのエクスポート変更

`apps/api-server/package.json` に exports を追加:

```json
{
  "exports": {
    "./app": "./src/app.ts"
  }
}
```

### 環境変数の使い分け

| 環境     | NEXT_PUBLIC_API_URL     | 接続先                       |
| -------- | ----------------------- | ---------------------------- |
| ローカル | `http://localhost:3001` | api-server (Node.js)         |
| Vercel   | `/api`                  | Next.js API Routes (Node.js) |

## 成果物

| ファイル                                                | 説明                    |
| ------------------------------------------------------- | ----------------------- |
| `apps/web/src/app/api/[...route]/route.ts`              | Hono統合API Route       |
| `apps/web/src/app/api/[...route]/__dev__/route.test.ts` | AC検証テスト            |
| `apps/web/package.json`                                 | 依存関係追加            |
| `apps/api-server/package.json`                          | ./app エクスポート追加  |
| `.env.example`                                          | NEXT_PUBLIC_API_URL追加 |

## テストケース

### ローカル動作確認

- [ ] `pnpm dev` で両サーバーが起動する
- [ ] `curl http://localhost:3001/health` が成功する
- [ ] フロントエンドからAPIが呼び出せる

### Vercel動作確認

- [ ] デプロイ後 `/api/health` が成功する
- [ ] デプロイ後 `/api/onboarding/packs` が成功する
- [ ] フロントエンドからAPIが呼び出せる

## 依存関係

- 007-02-01（Vercel連携設定）が完了していること

## 注意事項

### Runtime選択

- 現在はNode.js Runtimeを使用（api-serverの依存によりEdge Runtime不可）
- Edge Runtime対応には以下の作業が必要:
  - pino → Edge互換ロガーへの置き換え
  - find-up/dotenv → 環境変数直接参照への変更
  - または api-server に Edge 用エントリーポイント追加

### CORSについて

- 同一オリジンのため、Vercel環境ではCORS不要
- ローカルではapi-serverのCORSミドルウェアが処理

## 実装状況

- **status**: completed
- **作成日**: 2026-01-31
- **完了日**: 2026-01-31

### 実装ノート

- Edge Runtimeを当初予定していたが、api-serverの依存（pino, find-up, dotenv等）が
  Node.js固有のためNode.js Runtimeを使用
- Edge Runtime対応は別Subtaskとして分離することを推奨
- `hono/vercel`はhonoパッケージに含まれており、別途@hono/vercelのインストールは不要
